<UserControl
    x:Class="lanlanlu_toolkit.Views.MonitorRow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:lanlanlu_toolkit.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Margin="0,6">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="100" />
        </Grid.ColumnDefinitions>

        <TextBlock x:Name="LabelText" VerticalAlignment="Center" 
                   Style="{ThemeResource BodyTextBlockStyle}"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        
        <!-- Masked Progress Bar -->
        <Grid x:Name="BarContainer" Grid.Column="1" Height="14" VerticalAlignment="Center" Margin="12,0">
            <!-- Background Track -->
            <Border Background="{ThemeResource SystemControlBackgroundBaseLowBrush}" 
                    CornerRadius="7" />
            
            <!-- Fill with Clipping Mask -->
            <Border x:Name="BarFill" 
                    Background="{ThemeResource AccentTextFillColorPrimaryBrush}" 
                    CornerRadius="7"
                    HorizontalAlignment="Stretch">
                <Border.Clip>
                    <RectangleGeometry x:Name="ClipMask" Rect="0,0,2000,14">
                        <RectangleGeometry.Transform>
                            <ScaleTransform x:Name="ClipScale" ScaleX="0" />
                        </RectangleGeometry.Transform>
                    </RectangleGeometry>
                </Border.Clip>
            </Border>
        </Grid>

        <TextBlock x:Name="ValueText" Grid.Column="2" 
                   HorizontalAlignment="Right" VerticalAlignment="Center"
                   Style="{ThemeResource BodyStrongTextBlockStyle}" />
    </Grid>
</UserControl>
