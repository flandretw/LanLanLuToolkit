<Page
    x:Class="lanlanlu_toolkit.Views.TestToolPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:lanlanlu_toolkit.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer IsScrollInertiaEnabled="False" ManipulationMode="System">
        <Grid Padding="36" HorizontalAlignment="Stretch">
            <StackPanel Spacing="16" MaxWidth="1600" HorizontalAlignment="Stretch">
                <TextBlock x:Name="PageTitle" x:Uid="TestToolPage_Title" Style="{ThemeResource TitleTextBlockStyle}" Margin="0,0,0,8" />

                <!-- GridView for Tool Cards -->
                <GridView x:Name="ToolsGridView" 
                          ItemsSource="{x:Bind Tools}"
                          SelectionMode="None" 
                          IsItemClickEnabled="False"
                          HorizontalAlignment="Stretch">
                    
                    <GridView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <ItemsWrapGrid Orientation="Horizontal" HorizontalAlignment="Left" MaximumRowsOrColumns="4" ItemWidth="360" ItemHeight="96" />
                        </ItemsPanelTemplate>
                    </GridView.ItemsPanel>

                    <GridView.ItemTemplate>
                        <DataTemplate x:DataType="local:ToolItem">
                            <!-- Using Button as the card to get perfect hover and corner radius behavior -->
                            <Button HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                                    HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                                    Padding="0" Margin="0,0,12,12"
                                    BorderThickness="0"
                                    Background="Transparent"
                                    CornerRadius="8"
                                    Click="ToolButton_Click"
                                    Tag="{x:Bind Tag}">
                                <Border Background="{ThemeResource LayerFillColorDefaultBrush}"
                                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        Padding="24,12,12,12">
                                    <Grid ColumnSpacing="20">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        
                                        <!-- Left: Icon -->
                                        <FontIcon Glyph="{x:Bind Icon}" FontSize="32" Foreground="{ThemeResource AccentFillColorDefaultBrush}" VerticalAlignment="Center" />
                                        
                                        <!-- Right: Text Content -->
                                        <StackPanel Grid.Column="1" Spacing="4" VerticalAlignment="Center">
                                            <TextBlock Text="{x:Bind Title}"
                                                       Style="{ThemeResource BodyStrongTextBlockStyle}" 
                                                       TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" />
                                            <TextBlock Text="{x:Bind Description}"
                                                       Style="{ThemeResource CaptionTextBlockStyle}" 
                                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" 
                                                       TextWrapping="Wrap" MaxLines="2" />
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </Button>
                        </DataTemplate>
                    </GridView.ItemTemplate>

                    <GridView.ItemContainerStyle>
                        <Style TargetType="GridViewItem">
                            <Setter Property="Margin" Value="0" />
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="VerticalContentAlignment" Value="Stretch" />
                            <Setter Property="MinWidth" Value="0" />
                            <Setter Property="MinHeight" Value="0" />
                            <!-- Disable default GridViewItem visuals -->
                            <Setter Property="Background" Value="Transparent" />
                            <Setter Property="BorderBrush" Value="Transparent" />
                        </Style>
                    </GridView.ItemContainerStyle>
                </GridView>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
